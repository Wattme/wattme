import React from "react";
import {
  View,
  StyleSheet
} from "react-native/index";
import {

} from "react-native-ui-lib";

class DiagramBalance extends React.PureComponent {
  render() {
    return (
      <View style={styles.card}>

        

      </View>
    );
  }
}

const styles = StyleSheet.create({
  card: {
    borderWidth: 1,
    borderStyle: "solid",
    borderColor: "#F0F0F0",
    borderRadius: 14,
    padding: 16,

    backgroundColor: "#FFFFFF"
  }
});

export default DiagramBalance
